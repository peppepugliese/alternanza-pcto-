const banner = document.getElementById('banner-off');
banner.style.display = 'none';

const cross = getElementById by

setTimeout(() => {
    banner.style.display = 'block';
}, 5000);
let minutes = 10;
let seconds = 0;

const interval = setInterval(() => {
    seconds = 59;
    minutes--;
}

minute.Print()
)